version: '2'
services:
  taxon-web:
    build: .
    environment:
      - DB_URL=taxon-mongo
    ports:
      - "8597:8597"
    depends_on:
      - taxon-mongo
  taxon-mongo:
    image: mongo
    ports:
      - "27017:27017"
    command: --quiet
